Architecture

This project implements a simple HTTP reverse-proxy load balancer in Go with round-robin and least-connections algorithms. Components live under internal/ and the entrypoint under cmd/loadbalancer.
